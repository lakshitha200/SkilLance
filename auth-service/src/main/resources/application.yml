spring:
  application:
    name: AUTH-SERVICE

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
#      defaultZone: http://localhost:8761/eureka/
