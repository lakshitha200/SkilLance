spring:
  application:
    name: order-service

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka/
#      defaultZone: http://localhost:8761/eureka/
